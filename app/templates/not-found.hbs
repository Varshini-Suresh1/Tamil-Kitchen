<div class="page-container">
  <h1>Page Not Found</h1>
  <p>The page you are looking for does not exist. Please check the URL or return to the {{#if model}}
    {{#if (eq model.role 0)}}
      <button {{action "goToAdminDashboard"}}>Go to Dashboard</button>
    {{else if (eq model.role 1)}}
      <button {{action "goToResOwnerDashboard"}}>Go to Dashboard</button>
    {{else}}
      <button {{action "goToCustomerDashboard"}}>Go Home</button>
    {{/if}}
  {{else}}
    <p class="loading">Loading your access details, please wait...</p> <!-- Optional loading state -->
  {{/if}}
  </p>
</div>